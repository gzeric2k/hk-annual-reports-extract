# 两栏识别说明

## 检测流程

1. 提取页面词元及坐标（`extract_words()`）
2. 以页面中线为基准拆分左右词元
3. 判断左右词元数量是否充足（>=10）
4. 判断中缝间距是否足够（默认 >=6）
5. 若满足条件，按左右栏 bbox 分别提取并拼接

## 防误判保护

- 左右有效行数不足时回退单栏提取
- 左右提取字符总量明显低于整页时回退单栏提取
- 对左右栏拼接边界做重叠去重，减少重复段落

## 输出格式

双栏内容采用：

```
<左栏文本>
<<<COLUMN_BREAK>>>
<右栏文本>
```

后续逻辑会按该标记继续处理目录或正文。
